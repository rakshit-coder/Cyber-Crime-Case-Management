SECRET_KEY=django-insecure-dev-key-change-in-production-securely
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,*

# MySQL Configuration
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
DB_USER=root
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=3306

# Email Configuration (SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=rakshit7179@gmail.com
EMAIL_HOST_PASSWORD=byhnrbaifpbpyvxn

# Encryption
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef

# Base URL for email links and external access
# Set to your actual server address (not localhost for external devices)
# Examples: http://192.168.1.100:8000 or http://your-domain.com or http://ip-address:8000
BASE_URL=http://127.0.0.1:8000

# Security
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
